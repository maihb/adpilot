# 🔴 凭据：本仓库面向公开，密钥一律不进 git
# 「先放私有仓库以后清理」是陷阱——历史会把密钥带走，清理要重写全部 commit hash
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# 运行期数据：真实客户数据只在数据库里，不在仓库里
data/
uploads/
imports/
*.db
*.sqlite3

# RAG 语料由使用者运行时导入，不随代码分发
corpus/
!corpus/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
dist/
build/
*.egg-info/

# Node / uni-app
node_modules/
unpackage/
.hbuilderx/
*.local

# 构建产物
frontend/dist/
client/dist/

# IDE / OS
.idea/
# .vscode 下只放「clone 下来就该是对的」那两项：解释器指向项目 .venv、
# 格式化器用 ruff。个人偏好仍然留在各自的全局设置里，不进仓库。
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.DS_Store

# Claude Code：项目级配置与钩子进仓库（它们是这个仓库护栏的一部分），
# 本机私有覆盖不进 —— settings.local.json 里常有绝对路径和个人偏好
.claude/settings.local.json

# Celery beat 的排期状态文件。它记着「上次是什么时候投的」，是本机运行时状态，
# 不是配置。Makefile 里的 beat target 已经把它落到 /tmp，这几条是给「直接跑
# celery beat 没带 --schedule」的情况兜底。
celerybeat-schedule*
celerybeat.pid
