# <领域名>

> 新增领域时把这份拷走改名，然后删掉本行和下面所有尖括号提示。
> 别删小节 —— 某一节没有内容就写「暂无」，那本身也是信息（读的人不用再去翻源码确认）。

**代码**：`models/<x>.py`（表）· `schemas/<x>.py`（出入参）· `services/<x>.py`（业务）·
`api/<x>.py`（路由）· `tests/test_<x>.py`（测试）

**范围出处**：[设计文档 §<n>](../design/2026-08-19-mvp-design.md) · 里程碑 D<n>–D<n>

## 一句话职责

<这个领域负责什么、不负责什么。「不负责」往往比「负责」更有用。>

## 接口

| operationId | 路径 | 说明 |
|---|---|---|
| | | |

<没有对外接口就写「无，仅供内部服务与任务调用」。>

## 能读文档就够的部分

| 规则 | 一句话 |
|---|---|
| | |

<一行一条。每条都是「不看源码也能照着写对」的业务规则；写不成一行的，说明它属于下一节。>

## ⛔ 必须读源码的部分

<只列真正推不出来的：依赖执行顺序、事务边界、并发或安全考量的那几段。
每条都要写明**为什么**不看代码会写错 —— 写不出这个「为什么」，它就该挪到上一节。
一处都没有就写「暂无」。>

## 已知残余

<还没做完的、按假设实现的、等外部条件才能定的。每条写清：现在怎么做的、
答案变了要改哪个函数。口径类的假设同时去 glossary.md 记一笔「⚠️ 待定」。>
