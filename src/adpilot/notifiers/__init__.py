"""出站通知：把系统发现的事推出去。

和 `providers/`（入站：外部报表怎么进来）对称，这一层管的是出站。MVP 只有一个
实现（通用 webhook），但通道一定会增加 —— 客户端推送（D9–D11）、邮件、企业微信
应用消息。先把位置定下来，正是分层契约开着 `exhaustive` 想要的效果：新增一个顶层
模块必须先想清楚它摆在哪一层。

这一层的规矩：

* **只负责送出去，不决定要不要送。** 「这条告警值不值得推」是 `services/` 的判断。
* **失败不抛给调用方。** 推送失败不该让巡检任务失败 —— 告警已经落库了，人打开
  清单还是看得到。所以这里返回布尔值，不返回异常。
* 🔴 **URL 本身是凭据。** 企业微信、钉钉、飞书的 webhook 地址里直接带着 key，
  泄一次等于把发消息的权限交出去。所以它在 `Settings` 里是 `SecretStr`，
  **任何日志、任何异常消息里都不许出现**。
"""
