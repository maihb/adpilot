"""规则引擎：确定性计算，**不经 LLM**。

[设计文档第五节](../../../docs/design/2026-08-19-mvp-design.md)那条边界的落点：
**能用规则算的绝不用 LLM。** 余额可撑天数、库存断货预警、指标环比阈值全是确定性
计算，LLM 只负责解释和表达，不负责判断和计算。这条不只是省钱 —— 它让系统的关键
路径（要不要发告警）可单元测试。

这一层的形状由一条硬规矩决定：**只依赖数据，不依赖 IO。**

* 收的是**已经查好的**数据结构，返回判定结果，**不自己查库**；
* 因此可以用一张表格式的参数化测试把边界覆盖完，不必起数据库；
* 分层契约里 `rules` 在 `services` **之下**，且它够不着 `models` / `schemas` /
  `db` —— 想在这里写一句 `select(...)` 会被 import-linter 当场拦下。

口径的真相源是 [glossary](../../../docs/business/glossary.md)，不是这里的代码。
公式改了先改那篇，再改这里。
"""
