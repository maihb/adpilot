"""认证：token 的签发与校验、运营密码的哈希与比对。

**这一层够不着数据库，是刻意的。** 分层契约把 `auth` 压在 `rules` 同层 —— 它只
认识 `config`，所以想在校验里写一句「查一下这个 token 撤销没有」会被 import-linter
当场拦下。那正是[设计文档第四节](../../../docs/design/2026-08-21-client-auth.md)
「自包含 token 不可撤销」这个决定的机器形态：不是靠人记着别写，是写不出来。

需要状态的那一半（邀请码：能作废、能列出来）走正常的 `models` → `services`
分层，不进这里。
"""
